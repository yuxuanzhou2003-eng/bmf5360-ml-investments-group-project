# AI 产业链多层研究：当前入口

记录时间：2026-09-12 07:04 UTC。状态：研究结构已确定，底表覆盖审计进行中；不是新模型运行结果。

## 研究对象与投资决策

产业链名单定义可研究对象，不定义必须持有的股票或权重。股票×交易日是最小研究单位；可向上汇总至赛道和行业。等权仅为比较基准。模型可以决定行业总仓位、赛道权重和个股持仓，不要求每个模块最终都进入策略。

2021 年起作为本轮研究窗口；2021–2023 用于训练研究，2024–2025 已经被先前模型评估使用，只能视作已接触的开发验证期。2026 上半年继续保留为最终测试期，不在本阶段访问目标。不得将改模型之后的验证当作全新、一次性确认性检验。

## 四张研究表

1. 企业表：稳定证券标识、literal RIC、公司名、旧主赛道、建议细分赛道、多业务标签、分类证据、证据可得日、覆盖起止、上市/退市日期及各字段核验状态。AI 可协助分类，业务事实必须有来源；当前描述不能证明历史当时的业务。
2. 股票日表：证券标识×交易日、行情与流动性、过去可知的技术指标、当时可知的财务数据、缺失原因、来源版本与最晚输入时间。保留原行；不先对股票求平均，也不沿用旧 non_overlap_selected 作为通用筛选条件。
3. 事件表：事件ID、主体、类型、原始公告时间和时区、首次可得时间、来源、事件发生前预期、公告内容、修订状态。日期不精确或缺来源的事件标为待核验。未取得来源不等于没有事件。
4. 赛道日表：从股票表按明确、滞后的成员规则生成趋势、广度、分散度和风险指标；用于研究轮动。统计汇总不等于可执行投资组合，组合必须另有持仓账本。

未来标签单独保存，并记录入场/退出时点。特征构建不读取未来标签。股票ID、事件ID、日期不能作为无意的模型预测因子。

## 赛道细化提案（尚未执行公司重分类）

旧七组保留，增加细分层候选：加速器/处理器、配套芯片、存储/HBM、服务器、网络/光互连、云算力运营、数据中心运营、电力/配电、冷却、软件/EDA。机器人/自主系统保留为独立扩展研究组，不因此前篮子排除就从主企业表删除。

截图只是来源之一，目前 49-RIC 旧名单不是截图逐项复刻。截图的海外及新增公司应进入候选核验队列，不能因为本地未覆盖就默认为不存在或不相关。公司可有多个业务标签；组合层需要防止多标签重复计权。

## 首轮研究顺序

先审计日期/证券覆盖与成员时间含义，修复既有计算错误，再建立日表。第一轮以赛道为单位检查趋势、风险、广度和宏观敏感度。随后比较同赛道个股差异。财报事件优先使用已有可核验来源；资本开支指引、产品发布等未核验事件暂不进入模型。

事件分析先问事件后反应是否稳定，再决定是否把事件特征用于预测。公告前模型不能使用公告后的 actual 或 surprise；公告后模型必须等待可交易时点。上下游影响首先是关联研究，不自动声称因果关系。

分类、收益回归和排序各自对应不同目标；不预设 Logistic 是最终模型。不一次性组合所有模块：先设简单基准，再在训练期按因子组验证增量，并明确记录全部尝试。

## 必须修复后才可继续的计算问题

详见 MODEL_CALCULATION_REVIEW_20260912.md：训练标签跨界 purge、广度缺失分母、超额动量复利定义、标准 Sharpe、成交时点和期初可知持仓、日内/持有期内回撤及完整成本。非重叠窗口仍可能相关，日频行数不代表独立样本数。

## 已定位来源

- 企业候选：data/audit/ai_pool_expansion_v1/20260910T045000Z/candidate_registry.csv。
- 日表候选：data/model_ready_ai_pool_daily_v1/20260910T060000000000Z/ 下的 model_features.csv、metadata.csv、eligibility.csv。旧资格列不能未经复核直接决定新研究的可交易资格。
- 财务：data/clean/ai_factor_v1/20260910T024500000000Z/fundamental_observations.csv。已发现 unit_status/currency_status 等阻断状态；不能直接将未核验字段投入模型。
- 事件候选：data/panel_v3/20260909T035245922341Z/events.csv。这里只定位文件，尚未完成本轮字段和覆盖核验。

## 本阶段不作的声明

没有新增预测或收益结果；没有完成截图名单映射；没有完成全量财务和事件数据审计。既有 v2 AUC 和收益指标维持待修复状态，不用于淘汰或采用模型。
