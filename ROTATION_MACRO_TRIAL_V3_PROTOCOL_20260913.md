# 赛道宏观试跑 v3：运行前规则

日期：2026-09-13。状态：在本轮输出出现前登记；2024–2025 已在旧研究使用，本轮属于探索性开发，不是全新确认性预注册。

## 目标与信息时点

以现有 49 家、7 主赛道静态 AI 候选池试跑。形成日 t 收盘后计算信号，t+1 收盘入场，t+6 收盘退出；五日收益用 t+2 至 t+6 的日总收益，逐股票复合后按形成日等权。AI 参考收益为七个赛道固定初始等权的平均五日收益。分类标签是赛道收益减参考收益是否大于零。本轮不是 SPY 超额目标。

成员仅依据 t 日已知的有限收益（>=-100%）和正收盘价确定；未来完整性只影响目标是否可评价，不改变成员、特征或预测。停牌/退市/公司行动账本仍待专项核验，不声称可实盘收益。原始 61,495 股票日行、49家公司全部保留；所有新输出使用独立目录。

形成日成员在入场与退出日也须有正且有限收盘价，才可计算该目标；缺失导致目标不可评价并记录成员及原因，不根据未来缺口重选成员。此规则只定义诊断可评价性，不冒充基金遇到停牌/退市后的清算规则。

形成日全集先构造技术特征的同日 z-score，宏观按 Date 对接前一 SPY session 的已有宏观值；禁止先按未来目标筛选。宏观沿用旧 as-of 携带，记录覆盖、年龄、来源日期，不新增填充。信用利差历史不足，本轮排除；不会当作零。

为保证对照始终有同样七个赛道，每日任一赛道技术或所选宏观缺失，则整日不进入本轮特征可用集合，并保留原因。预测对所有特征可用验证日期生成；目标缺失仅令该日评价缺失，不令预测消失。全部日期和排除记录留存。

## 固定因子与模型

- F0 七个技术因子：subsector_return_1、momentum_5/20/60、volatility_20_ann、breadth_positive_20、dispersion_1。注意 volatility_20_ann 沿用旧字段，含义是成员股票波动的平均，不是赛道组合波动。
- F1：F0 加 vix_level、vix_change_5d、dgs2_level、dgs2_change_20d、dgs10_change_20d、term_spread_10y_2y、broad_dollar_change_20d、dgs3mo_level。宏观只在训练期标准化，不作当日横截面 z-score。
- F2：F1 加四个明确交互：标准化 VIX 水平×技术波动 z-score；VIX 五日变化×五日动量；10年利率20日变化×60日动量；美元20日变化×20日动量。它们是风险状态改变趋势关系的假说，不冒充历史利率敏感度估计。
- Logistic：C=0.5，无类别重加权，max_iter=2000。
- Random Forest：300 trees、max_depth=4、min_samples_leaf=60、max_features=0.7、seed=5360。两模型×三因子组，共六次固定配置，不在验证期调参。
- 所有变换只在训练样本拟合；保存模型、变换器、系数/重要性和版本。训练2021–2023、验证2024–2025。训练目标跨入验证期者按真实 target_end_session purge。2026目标不访问。

## 评价与推断

主预测指标为日均 rank IC 和同日成对排序正确率。pooled AUC、Brier、准确率为辅助；不将跨日 AUC 增量自动解释为轮动。分别报2024、2025。

经济诊断为每日选择前三赛道各1/3的五日平均超额收益，对照简单20日动量选前三和等七赛道。排序在完整特征可用集合完成，不根据未来标签调整持仓；字典序打破分数并列。该诊断不是完整基金，不执行股票10%限额，不计算净收益/Sharpe，不把重叠五日收益复利。

推断用共同连续验证交易日历的 moving-block bootstrap，同步抽取所有模型/赛道。主块长20日，敏感性5/10/40日；1000次，seed=5360；缺失评价日期保留在日历，指标内部忽略其缺失值。报告IC/排序/前三超额均值区间、F1-F0和F2-F0配对差、相对动量差；pooled AUC差可用主块长同步配对抽样。完整列出六配置，不只挑最好者。

## 下一阶段边界

这次先检验宏观能否改善轮动。行业择时、真正逐日持仓与现金计息、成本/冲击、个股限额、事件叠加和最终留出测试不由本次诊断代替。若当前样本或数据问题阻止可信推断，明确报告，不用更复杂模型掩盖。
